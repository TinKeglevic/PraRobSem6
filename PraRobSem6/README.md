# PraRobSem6
GitHub repozitorij za seminar grupe 6 na FERovom kolegiju Praktikum robotike 2026.


sudo apt install ros-jazzy-ros2-control
sudo apt install ros-jazzy-ros2-controllers
sudo apt install ros-jazzy-dynamixel-*
sudo apt install ros-jazzy-xacro
sudo apt install ros-jazzy-rqt-joint-trajectory-controller

sudo usermod $USER -a -G dialout
sudo chmod 777 /dev/ttyUSB0